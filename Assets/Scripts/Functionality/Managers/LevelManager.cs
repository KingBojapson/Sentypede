using System.Collections;
using System.Collections.Generic;
using UnityEngine;

//I haven't implemented this yet and probably should it would be easier to keep tabs on level controls
public class LevelManager : MonoBehaviour
{
    void Start()
    {

    }
    public enum LevelState
    {
        FirstStage = 0,
        SecondStage = 1,
        ThirdStage = 2,
        FourthStage = 3
    }
}
